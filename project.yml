name: ChromaticCamera
options:
  projectFormat: xcode15_3
  deploymentTarget:
    iOS: "15.0"
targets:
  ChromaticCamera:
    type: application
    platform: iOS
    sources: [ChromaticCamera]
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: com.ilyalevchenko.Chromatic
      PRODUCT_NAME: ChromaticCamera
      INFOPLIST_FILE: ChromaticCamera/Info.plist
